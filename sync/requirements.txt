gspread==6.1.2
google-auth==2.32.0
# Bumped from 2.7.4 for google-genai below: 2.7.4 pins httpx<0.28, but
# google-genai requires httpx>=0.28.1 — mutually exclusive, so this floor
# is required, not just a nice-to-have upgrade.
supabase==2.31.0
google-genai==1.47.0
